/**
 * Root Store
 */
class RootStore {
    constructor() {
        this.state = {
            // ADD state variable here
            // Will be available in context as stores.rootStore.state.var
        };
        this.version = '0.29';
    }
}

export default RootStore;
